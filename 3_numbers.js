var age = 27;

// age = age + 1
age += 1;

var weight = 75;

// weight = weight - 2
weight -= 2;

var sandwich = 1;

weight = weight + sandwich;

var playSoccer = 3;

weight -= playSoccer;

var winePrice = 200.3;

var total = Math.round(winePrice * 100 * 3) / 100;
var totalStr = total.toFixed(3);
var total2 = parseFloat(totalStr);

var pizza = 8;
var person = 2;

var countPiecesByPerson = pizza / person;
